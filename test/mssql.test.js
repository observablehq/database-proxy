import {expect} from "chai";

describe("mssql", () => {
  describe("check", () => {
    it("should test", async (done) => {
      expect(1).equal(1);
      done();
    });
  });
  describe("query", () => {
    it("should test", async (done) => {
      expect(1).equal(1);
      done();
    });
  });
});
